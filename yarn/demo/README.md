# Yarn Demo Project
To compile, you'll need `ysc` installed (`YarnSpinner.Console`).

To generate a new version of `Project.yarnc`, run:
```
ysc ./Project.yarnproject
```

That produces a protobuf which can be loaded by the Zig runtime.
